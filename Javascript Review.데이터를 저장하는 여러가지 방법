1.데이터를 저장하는 여러가지 방법
  
  1) 일반변수
    숫자,
    문자,
    불린,
    
    객체,
    배열,
    함수

  2) 연산자 
    ++, --
    +=, -=, *=, /=
    &&, ||, !
    >, >=, <, <=, ==, ===

    let some = (num>100) ? a : b;

  3) 입출력
    window.prompt()
    prompt()

    document.write()
    console.log()

  4. "문자형숫자"->진짜숫자 변환함수
    Number()
    parseInt()

  5. 제어문
    단순 if
    if~else
    다중 if

    switch문 break

  6. 반복문
    for(초깃값;조건식;증감식){}
    while
    do~while

  7. 객체
    형태 => key : value
    
    property    
    속성 
    멤버변수

    멤버함수
    메소드

  8. 객체를 만드는 방법 2가지
    1) {}, new Object();
    2) 생성자함수 (사용자정의 데이터타입)

  9. 참조변수(참조값) - 레퍼런스 주소값
    객체 - new Object(), {}
    배열 - new Array(), []
    함수 - new Function(), function(){}

  10. 함수 - 일급객체
    일괄 반복처리(코드의 재활용)
    1)일만하는 함수(매개변수x,리턴값x)
    2)매개변수를 가진 함수
    3)리턴값이 있는 함수
      return : 호출한 곳으로 값을 되돌려 준다.
               함수 종료
    4)선언식함수 function 함수명(){}
    5)표현식함수 
      const myFn = function(){};

  11. 함수와 메소드 - 사용형태로 구별
      window.alert() <- 메소드
      alert() <- 함수라고 부른다.

  12. 자바스크립트 내장객체
    = JS에서 기본적으로 제공하는 생성자함수

    Math
    new Date();
    new String();
    new Number();
    new Boolean(); ....
     
  13. DOM = 문서객체모델 => 태그
    JS관점 DOM(태그)의 역할? 
      JS세계에서 HTML문서의 내용을 조작하는 통로(관문, 인터페이스) 역할

    document.getElementById()
    document.querySelector() - 단일요소
    document.querySelectorAll() - 여러개(배열형태[인덱스])
    
    $ul.setAttribute('속성명','값');
    $ul.getAttribute('속성명');

    $ul.style.color='red';
    $ul.style.setProperty('속성명','값');
    //css속성을 가져오는 방법
    window.getComputedStyle('DOM').getPropertyValue('속성명');

  14. 특정DOM에 이벤트 등록  
  $ul.addEventListener( 동작, 함수 )
  $ul.addEventListener('click',function(){})

  15. 지금 우리에게 절실히 필요한 것
    =>알고리즘(로직) 구성 능력
    =>자연어 로직 구성
    =>자연어 로직을 코드로 표현하는 능력